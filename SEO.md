# استراتيجية SEO — محل حسن للإلكترونيات (خميس مشيط)

## 1. الاستراتيجية العامة
موقع خدمة محلية عربي أولاً (RTL) يستهدف نيات البحث المحلية والعضوية في **خميس مشيط، السعودية**، لخدمات: تركيب وصيانة الدش، الدش المركزي، برمجة الرسيفرات، الشاشات، كاميرات المراقبة، وإعداد أجهزة IPTV. النهج people-first: محتوى مفيد + بيانات نشاط متسقة + بنية تقنية نظيفة. لا قبعة سوداء.

## 2. الموقع الأساسي
- الأساسي: **خميس مشيط** (منطقة عسير).
- الثانوي (فقط إن تأكدت التغطية): **أبها** — حالياً `SERVES_ABHA` في `src/config/business.ts` يحتاج تأكيداً.
- رابط خرائط Google الموحّد: `https://maps.app.goo.gl/vL2v7rJFLw8nhcPx8`

## 3. مجموعات الكلمات حسب النية
- **تجارية محلية**: فني دش خميس مشيط، تركيب دش خميس مشيط، صيانة دش خميس مشيط، تركيب دش مركزي، فني رسيفر، تركيب شاشات، تركيب كاميرات مراقبة، IPTV خميس مشيط.
- **حل مشكلة**: لا توجد إشارة، القنوات اختفت، إشارة ضعيفة، تقطيع القنوات، ضبط نايل سات/عرب سات، رسيفر يحتاج برمجة.
- **محل / بيع**: محل دش ورسيفرات، بيع رسيفرات وأطباق دش.
- **قرب**: تُخدم عبر LocalBusiness schema + خرائط + صفحة الموقع + GBP، وليس عبر تكرار «قريب مني».

## 4. ربط الـ URL بالكلمات
انظر `SEO-KEYWORD-MAP.md` (جدول كامل). كل كلمة رئيسية لها URL واحد لتفادي تنافس الصفحات.

## 5. استراتيجية العناوين والوصف
- قالب العنوان: `%s | محل حسن للإلكترونيات` (معرّف في `src/app/layout.tsx`).
- كل صفحة: عنوان فريد ≤ ~60 حرفاً مرئياً + وصف عربي طبيعي يذكر الخدمة + المدينة + دعوة للاتصال/واتساب.
- OpenGraph/Twitter بالعربية، ورابط OG = canonical.

## 6. البيانات المنظمة (Schema)
مُولّدة مركزياً في `src/config/seo.ts`:
- `LocalBusiness` + `WebSite` — في `layout.tsx` (كل الصفحات).
- `FAQPage` — الرئيسية، صفحة الموقع، ومقالات الدليل.
- `Service` + `BreadcrumbList` — كل صفحة خدمة (`components/ServiceJsonLd.tsx`).
- `Article` + `BreadcrumbList` — مقالات الدليل.
- لا `aggregateRating` / `review` ما لم توجد تقييمات حقيقية ظاهرة.
- لا `streetAddress`/`geo` مُختلقة — القيم الحالية منقولة وتحتاج تأكيداً (`SEO-CHECKLIST.md`).

## 7. تكامل خرائط Google
- زر «عرض الموقع على خرائط Google» (`MapsButton`) في: الرئيسية (قسم الموقع)، الفوتر، صفحة `/location/khamis-mushait`، صفحة القسم `location.tsx`، وشريط الجوال.
- الخريطة المضمّنة (iframe) الحقيقية محفوظة في `GOOGLE_MAPS_EMBED`. لم يتم اختلاق أي iframe من الرابط المختصر.

## 8. استراتيجية LocalBusiness
- NAP موحّد من `src/config/business.ts` في كل الموقع.
- `areaServed`: خميس مشيط (+ أبها عند التأكيد).
- `hasMap` يشير إلى رابط خرائط Google الموحّد.
- ساعات العمل، الهاتف، الموقع — كلها من مصدر واحد.

## 9. الربط الداخلي
- الرئيسية → كل الخدمات + صفحة الموقع + الدليل.
- كل صفحة خدمة → `RelatedServices` (بقية الخدمات + صفحة الموقع + الدليل) + Breadcrumbs.
- كل مقال دليل → صفحات الخدمة ذات الصلة + بقية المقالات.
- الفوتر → روابط خدمات + صفحة الموقع + الدليل + خرائط Google.
- Anchors عربية وصفية (لا «اضغط هنا»).

## 10. صفحات الخدمات
`/services/installation` · `dishmaintenance` · `dishrepair` · `centraldish` · `receivers` · `screens` · `cameras` · `customersupport`
تم: تنظيف العناوين من الحشو، تحويل العنوان الفرعي من `<h2>` إلى `<p>`، إضافة Breadcrumbs + Service/Breadcrumb JSON-LD، استبدال قسم «كلمات مفتاحية» بقسم روابط داخلية.

## 11. استراتيجية المحتوى
- محتوى عربي طبيعي احترافي، بلا فقرات حشو.
- صفحة `/location/khamis-mushait`: خدمات + مشاكل شائعة + موقع + FAQ.
- الدليل `/guides`: 6 مقالات تجيب عن سؤال المستخدم أولاً ثم تربط بالخدمات.
- أفكار مستقبلية: «تكلفة تركيب الدش المركزي للعمائر»، «الفرق بين رسيفر HD وIPTV»، «كيف تختار كاميرا مراقبة لمحلك»، «جدول ترددات القنوات الشائعة على نايل سات»، دراسات حالة بصور حقيقية.

## 12. SEO التقني
- Next.js 15 App Router + `output: export` (HTML ثابت) — كل المحتوى والـ metadata مرئي للزواحف بدون JS.
- عنوان/وصف/canonical لكل صفحة، robots.ts، sitemap.ts.
- `lang=ar dir=rtl`، HTML دلالي، عناوين متدرّجة، alt للصور، روابط وصفية.
- لا noindex عرضي، لا canonical مكرر، لا روابط داخلية مكسورة.

## 13. Sitemap
`src/app/sitemap.ts` — الرئيسية + صفحة الموقع + 8 خدمات + الدليل + 6 مقالات. لا صفحات إدارية/تجريبية/باراميترات.

## 14. robots
`src/app/robots.ts` — `Allow: /` للجميع + `Host` + `Sitemap`. لا حظر لأي أصول عرض.

## 15. الأداء
- خط Tajawal عبر `next/font` (self-hosted، `display: swap`).
- إزالة Font Awesome وسكربت GA المكرر.
- صورة الـ Hero: `bg-cover` — يُفضّل لاحقاً تحويلها WebP/AVIF وتقليل حجمها.
- الصور تحت الطية: `loading="lazy"` (iframe الخريطة كذلك).
- لا preload لموارد غير ضرورية.
- **متابعة**: قياس LCP/CLS/INP بعد النشر وتحسين صور `public/image/`.

## 16. إعداد Google Search Console
انظر `SEO-GOOGLE-BUSINESS.md` القسم 11.

## 17. إعداد Google Business Profile
انظر `SEO-GOOGLE-BUSINESS.md` كاملاً.

## 18. أفكار محتوى مستقبلية
مذكورة في القسم 11 أعلاه + صفحة أبها عند تأكيد التغطية + صفحة «الأسعار التقريبية» عند توفّر بيانات أسعار حقيقية.

## 19. معلومات تحتاج تحقق يدوي
كل البنود في `SEO-CHECKLIST.md` قسم «معلومات تحتاج تحقق يدوي» — أهمها: اسم النشاط (حسن/صالح)، العنوان الدقيق، الإحداثيات، الرمز البريدي، تغطية أبها، صحة الأسعار الظاهرة، البريد الإلكتروني الرسمي.

## أين تُعدّل الإعدادات
- بيانات النشاط والموقع: `src/config/business.ts`
- مولّدات الـ Schema: `src/config/seo.ts`
- مكونات CTA / روابط داخلية / breadcrumbs: `src/components/cta.tsx`
